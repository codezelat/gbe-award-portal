import {and,eq} from "drizzle-orm";import {notFound} from "next/navigation";import {getDb} from "@/lib/db";import {applications,payments} from "@/lib/db/schema";import {requirePortalSession} from "@/server/dal/auth";import {AuthenticatedUpload} from "@/components/uploads/authenticated-upload";
export default async function ReplacePayment({params}:{params:Promise<{applicationId:string}>}){const {applicationId}=await params;const {profile}=await requirePortalSession();const [row]=await getDb().select({application:applications,payment:payments}).from(applications).innerJoin(payments,eq(payments.applicationId,applications.id)).where(and(eq(applications.id,applicationId),eq(applications.ownerProfileId,profile.id),eq(payments.status,"rejected"))).limit(1);if(!row)notFound();return <><h1 className="page-heading">Replace payment proof</h1><p className="mt-2 max-w-2xl text-graphite">Upload one replacement for {row.application.reference}. The previous proof remains in the authorised history and will no longer be current.</p><section className="surface mt-7 max-w-2xl rounded-lg p-6 md:p-8"><div className="mb-6 rounded-md bg-muted p-4 text-sm text-graphite"><strong>Reason:</strong> {row.payment.rejectedReason??"The finance team could not verify the previous proof."}</div><AuthenticatedUpload applicationId={applicationId} kind="payment_proof"/></section></>}
